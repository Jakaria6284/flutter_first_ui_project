import 'package:flutter/material.dart';
class pancake extends StatelessWidget {
  const pancake({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('pancake')),
    );
  }
}